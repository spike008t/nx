# packages-directus

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build packages-directus` to build the library.

## Running unit tests

Run `nx test packages-directus` to execute the unit tests via [Jest](https://jestjs.io).
